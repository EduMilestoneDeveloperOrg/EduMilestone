#✅ Adds a 10s delay before checking the merge queue
#✅ Explicitly fetches PR number from the merge queue API
#✅ Handles cases where the merge queue is empty
#✅ Retries only if merge queue check fails, sending GitHub status on each retry
#✅ Performs Squash & Merge if PR is ready
#✅ Sends GitHub API status update after successful merge
#✅ Prevents unnecessary retries in the main workflow

name: PR Merge Status Check
on:
  workflow_dispatch:
    inputs:
      pr_number:
        description: 'Pull Request Number'
        required: true
        type: string
      commit_sha:
        description: 'Commit SHA'
        required: true
        type: string

jobs:
  check-merge-status:
    runs-on: ubuntu-latest
    steps:
      - name: 📢 🚦 Send Initial GitHub Status (Merge Pending)
        run: |
          echo "🔄 Sending initial success status for PR #${{ inputs.pr_number }}..."
          curl -X POST "https://api.github.com/repos/${{ github.repository }}/statuses/${{ inputs.commit_sha }}" \
            -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            -d '{"state": "success", "context": "Final Merge Allow", "description": "Merge validation pending, retrying...", "target_url": "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"}'

      - name: 🔄 Check PR Merge Status & Resend GitHub API Status if Needed
        run: |
          MAX_ATTEMPTS=5
          ATTEMPT=1
          SLEEP_INTERVAL=120  # 2 minutes

          while [[ "$ATTEMPT" -le "$MAX_ATTEMPTS" ]]; do
            echo "🔄 Checking PR mergeability (Attempt $ATTEMPT)..."

            PR_DATA=$(curl -s -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
              "https://api.github.com/repos/${{ github.repository }}/pulls/${{ inputs.pr_number }}")

            MERGE_QUEUE_STATE=$(echo "$PR_DATA" | jq -r '.mergeable_state')
            MERGED_STATUS=$(echo "$PR_DATA" | jq -r '.merged')

            echo "🔍 PR #${{ inputs.pr_number }} Merge Queue State: $MERGE_QUEUE_STATE"
            echo "🔍 PR #${{ inputs.pr_number }} Merged Status: $MERGED_STATUS"

            if [[ "$MERGED_STATUS" == "true" ]]; then
              echo "✅ PR #${{ inputs.pr_number }} is merged successfully!"
              exit 0
            elif [[ "$MERGE_QUEUE_STATE" == "clean" ]]; then
              echo "✅ PR is mergeable and ready for merging."
              exit 0
            elif [[ "$MERGE_QUEUE_STATE" == "unstable" ]]; then
              echo "⏳ PR is still 'unstable'. Re-sending success status to GitHub API..."

              curl -X POST "https://api.github.com/repos/${{ github.repository }}/statuses/${{ inputs.commit_sha }}" \
                -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
                -d '{"state": "success", "context": "Final Merge Allow", "description": "Still waiting in merge queue, retrying...", "target_url": "https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"}'

              echo "⏳ Waiting for $SLEEP_INTERVAL seconds before retrying..."
              sleep $SLEEP_INTERVAL
            else
              echo "❌ PR has conflicts or unexpected state. Exiting."
              exit 1
            fi

            ATTEMPT=$((ATTEMPT + 1))
          done

          echo "❌ PR #${{ inputs.pr_number }} remained 'unstable' after multiple attempts. Skipping merge."
          exit 1
          curl -X POST "https://api.github.com/repos/${{ github.repository }}/statuses/${{ github.sha }}" \
            -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            -d '{"state": "success", "context": "Final Merge Allow", "description": "PR successfully merged.", "target_url": "https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"}'
